---
import { CheckCircle, Award, Users, TrendingUp } from "lucide-react";

const skills = [
  "Listen first, code second",
  "Focus on functionality and performance",
  "Build for growth and scalability",
  "Provide clear, jargon-free communication",
];

const achievements = [
  // { icon: Award, number: "50+", label: "Projects Completed" },
  // { icon: Users, number: "30+", label: "Happy Clients" },
  { icon: TrendingUp, number: "3", label: "Years Experience" },
];
---

<section id="about" class="py-20 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <div>
        <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
          About CPB Dev
        </h2>
        <p class="text-lg text-gray-600 mb-6 leading-relaxed">
          CPB Dev is a frontend development company specializing in user
          interface implementation and modern web applications. We work with
          businesses of all sizes to transform designs into high-quality,
          performant code using current industry standards and best practices.
        </p>
        <p class="text-lg text-gray-600 mb-8 leading-relaxed">
          Our team has extensive experience with React and other modern
          JavaScript frameworks. We focus on writing clean, maintainable code
          that scales with your business needs while ensuring cross-browser
          compatibility and optimal performance. We handle everything from
          converting design files to functional interfaces, building component
          libraries, modernizing legacy applications, and providing technical
          guidance on frontend architecture decisions. Our goal is to deliver
          reliable solutions that meet your technical requirements and business
          objectives.
        </p>

        <div class="grid grid-cols-2 gap-6 mb-8">
          {
            skills.map((skill) => (
              <div class="flex items-center">
                <CheckCircle className="h-5 w-5 text-green-500 mr-2 flex-shrink-0" />
                <span class="text-gray-700">{skill}</span>
              </div>
            ))
          }
        </div>
      </div>
      <div class="relative">
        <img
          src="https://images.pexels.com/photos/196644/pexels-photo-196644.jpeg?auto=compress&cs=tinysrgb&w=600"
          alt="Design workspace"
          class="w-full h-96 object-cover rounded-2xl shadow-xl"
        />
        <div
          class="absolute -bottom-6 -right-6 bg-white p-6 rounded-xl shadow-xl"
        >
          <div class="grid grid-cols-1 gap-4">
            {
              achievements.map((achievement) => (
                <div class="flex items-center">
                  <achievement.icon className="h-8 w-8 text-blue-800 mr-3" />
                  <div>
                    <div class="text-2xl font-bold text-gray-900">
                      {achievement.number}
                    </div>
                    <div class="text-sm text-gray-600">{achievement.label}</div>
                  </div>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
